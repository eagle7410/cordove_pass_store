import React from 'react';
import Layout from '../Components/AuthLayout/Layout'

const DataList = (state) => {
	return (
		<Layout title="Data List">
			<div>Many interesting here</div>
		</Layout>)
};

export default DataList
