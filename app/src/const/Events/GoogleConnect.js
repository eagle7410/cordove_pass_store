const prefix = 'GoogleConnect';

const GoogleConnect = {
	init         : `${prefix}IsInit`,
	isHaveConfig : `${prefix}SetToken`,
	toolsOpen    : `${prefix}ToolsOpen`,
	toolsClose   : `${prefix}ToolsClose`,
};

export default GoogleConnect;
